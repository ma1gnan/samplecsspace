property(
    property_id INTEGER PRIMARY KEY,
    address TEXT,
    city TEXT,
    school_district TEXT,
    bedrooms INTEGER,
    bathrooms INTEGER,
    price REAL,
    pool TEXT,
    seller_id INTEGER,
    listing_agent INTEGER
)